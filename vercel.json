{
    "buildCommand": "cd playground && npm run build",
    "outputDirectory": "playground/.next",
    "installCommand": "cd playground && rm -rf node_modules package-lock.json && npm install --include=optional"
}